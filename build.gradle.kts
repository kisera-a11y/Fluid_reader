// Top-level build file. Each module declares its own plugins (with versions) so that
// this environment's :core module can be configured/built/tested independently of the
// :app module, which requires the Android SDK.
